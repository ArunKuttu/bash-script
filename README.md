
This Bash script is used to find the duplicate IP's and Mac address in a DHCP server.

First run the ipcollect.sh and mac-collect.sh scripts

after that execute the check_duplicate-IP.sh for listing the duplicate IP list and check_mac_duplicates.sh for Listing the duplicate mac address
